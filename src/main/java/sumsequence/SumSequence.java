package sumsequence;

import java.util.Map;

public class SumSequence {

	/**
	 * @param inArray
	 *            is not null and inArray.length > 0
	 * @return a Map where the Map contains keys that are array indices and the
	 *         corresponding values are the lengths of the longest consecutive
	 *         subsequence that is a SumSequence starting at those indices.
	 */
	public static Map<Integer, Integer> getSumSequenceLengths(int[] inArray) {
		// TODO implement this method
		return null; // change this
	}

}
